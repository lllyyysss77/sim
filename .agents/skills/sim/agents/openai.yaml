interface:
  display_name: "Sim"
  short_description: "Repo-specific patterns and workflows for sim"
  default_prompt: "Use the sim repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true